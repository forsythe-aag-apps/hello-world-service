apiVersion: v1
kind: Service
metadata:
  name: health-check-service
  labels:
    app: health-check-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: health-check-service
  type: LoadBalancer